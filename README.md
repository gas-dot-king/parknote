# 아맞다주차!

"아, 맞다! 어디 댔더라?" 하는 순간을 위해, 주차하고 나오면서 버튼 한 번으로 위치를 기억해 두는 개인용 안드로이드 앱.
외부 라이브러리 0개, APK 약 97KB. 내 폰에만 설치해서 쓰는 용도.

## 기능 (v3.3)

### 주차하는 순간 — 놓치지 않기
- **끊기면 바로 초안**: 차량 블루투스가 끊기는 순간 폰이 확실히 아는 두 가지, **시각과 좌표**를
  구역 없는 기록("구역 미입력")으로 먼저 저장한다. 알림을 무시해도 남는다. 구역은 나중에
  알림 버튼·위젯·홈 격자·잠금 화면 빠른 입력 어느 경로로 눌러도 **그 초안에 채워진다** —
  새 기록이 생기지 않으니 내린 시각과 좌표가 그대로 보존된다
  (예전에는 이 두 사실이 알림 버튼 안에만 살아 있어, 알림 본문을 탭해 앱을 열고 격자를
  누르면 좌표가 지금 자리로, 시각이 지금으로 바뀌었다)
- **재연결 = 출차**: 블루투스가 다시 붙으면 그 기록에 출차 시각을 적고 출차 알림을 해제한다.
  히어로 카드가 "3:12 ~ 5:40 · 2시간 28분 주차"로 바뀐다. 끊긴 지 3분 안에 다시 붙었고 그새
  아무것도 안 적었으면 잠깐의 신호 끊김으로 보고 초안을 지운다
- **알림에서 직접 입력**: 버튼 두 개(자주 댄 순서)에 없는 구역은 알림의 입력 칸에 바로
  타이핑한다. 키보드 마이크로 말해도 된다. 앱을 열 필요가 없다
- **잠금 화면 위 빠른 입력**: 알림 본문을 탭하면 잠금을 풀지 않고 격자 전체가 뜬다.
  알림 탭 → 구역 탭 두 번으로 끝. 사진·음성 메모도 여기서 붙인다
- **끊긴 직후 단발 측위**: 캐시된 좌표가 없거나(내비를 안 켰다) 1분 넘게 낡았거나 오차가
  50m를 넘으면 8초만 새 좌표를 기다려 초안에 덮어쓴다. 지상이면 대개 잡히고 지하면 캐시로
  남는다. 주차당 한 번이라 배터리 영향은 없다시피 하다
- **자주 댄 순서**: 알림 버튼과 위젯의 구역은 최근순이 아니라 빈도순으로 고른다.
  어제 한 번 댄 자리보다 지난 스무 번 중 열네 번 댄 B2-A가 앞에 온다

### 사진 · 음성
- **사진 한 장**: 히어로 카드의 **사진**, 기록 수정의 **사진**, 빠른 입력의 **사진 찍기**.
  기둥 번호 표지판 한 장이 구역 이름보다 확실할 때가 있다. 히어로에 썸네일이 들어가고
  탭하면 크게 본다. 앱 전용 저장소에만 두고 갤러리에는 올리지 않는다(남의 번호판이 찍힌다).
  긴 변 1280px로 줄여 기록당 100~200KB. 기록이 지워지거나 잘려 나가면 사진도 지운다
- **음성으로 메모**: 시스템 음성 인식으로 "B3 기둥 47번 근처"를 말하면 메모에 붙는다.
  녹음 파일을 남기지 않는다 — 텍스트라 백업·검색·한눈에 읽기가 그대로 되고 권한도 필요 없다.
  음성 인식이 없는 기기에서는 버튼이 숨는다
- 백업 파일이 zip이 됐다 (`data.json` + `photos/`). 예전 JSON 백업도 그대로 읽는다

### 화면 구조 — 하단 탭 4개
- **홈 · 기록 · 위치 · 설정**으로 나뉜다. 예전에는 한 화면을 계속 스크롤하고
  나머지는 다이얼로그에 숨어 있었다 (전체 기록은 설정 메뉴 다섯 번째 항목이 유일한 통로였다)
- 탭 상태는 화면을 돌려도 유지되고, 알림·위젯으로 들어오면 홈으로 되돌아온다
- 홈이 아닌 탭에서 시스템 뒤로 가기를 누르면 먼저 홈으로 돌아가고, 홈에서 한 번 더 누르면 앱을 나간다

### 홈 탭
- **히어로 카드**: 지금 주차한 곳을 금색 면 + 자동차 그림으로 크게. 기록이 없으면
  평범한 카드로 내려앉는다 (금색은 "저장됨"의 신호라서). 차량 블루투스가 다시 붙으면
  (= 시동을 켰으면) "시동 켜짐 · 마지막 주차"로 내려앉는다 — 차는 더 이상 그 자리에 없다
- **감지 상태 카드**: 시동(차량 블루투스 연결) · 위치 판정을 한 줄씩 보여 준다.
  "왜 자동 알림이 안 왔지"를 앱 안에서 확인할 수 있는 유일한 자리 — 탭하면 설정 탭의 준비 상태로 간다
- 그 아래는 예전과 같다: 층 × 구역 격자 → 직접 입력 → 나가기 전 체크 → 최근에 댄 곳

### 위치 탭 — 내 차까지
- **거리 · 방향 · 도보 시간**: "152m · 북동쪽 · 도보 약 2분"
- **나침반 화살표**: 폰을 돌리면 차가 있는 쪽을 계속 가리킨다 (회전 벡터 센서)
- **좌표 표시·복사**: 역지오코딩이나 네트워크 요청 없이 저장된 위도·경도를 기기 안에서 표시한다
- **지도 앱으로 열기**: 사용자가 버튼을 누른 경우에만 `geo:` 인텐트로 좌표를 선택한 지도 앱에 넘긴다.
  이후 처리는 해당 지도 앱의 개인정보 처리방침을 따른다
- **지도 타일은 그리지 않는다.** 앱에는 `INTERNET` 권한·지도 SDK·Geocoder 호출이 없고,
  거리·방향 계산도 기기 안에서 수행한다
- 주차 기록에는 유효한 마지막 좌표와 측정 시각·정확도(`lat`/`lon`/`loc_t`/`loc_acc`)가 함께 저장된다.
  권한이 없거나 좌표가 유효하지 않으면 위치 필드는 붙지 않는다
- 30분보다 오래됐거나 오차가 50m보다 큰 좌표는 경고를 표시하고, 정확한 도착 판정과
  나침반 안내를 숨긴다
- **내 위치는 이 탭이 보이는 동안만 2초 간격으로 갱신**된다. 걸어가면 거리와 화살표가
  따라오고, 탭을 벗어나거나 앱이 백그라운드로 가면 측위와 센서를 함께 끈다
  (예전에는 탭을 연 순간의 좌표에 고정돼 100m를 걸어도 화살표가 점점 틀어졌다)
- 옛 기록·권한 없음·기록 없음 세 경우를 구분해 각각 다른 안내와 버튼을 띄운다

### 설정 탭 — 자동 감지 준비 상태
- 자동 기록에 필요한 조건 다섯 개를 **완료 / 확인 필요 / 사용 안 함**으로 한눈에 보여 준다
  - 알림 권한 · 차량 블루투스 등록 · 배터리 제한 없음 · 위치 권한 · 항상 위치 허용
- **설정하기**를 누르면 해당 시스템 화면으로 바로 간다. 배터리 최적화는 지금까지
  이 README의 "폰에 설치 후 할 일 4번"으로만 안내했는데, 앱을 깐 사람이 README를 읽을 이유가 없었다
- 위치로 알림 조절이 꺼져 있으면 위치 두 항목은 경고가 아니라 "사용 안 함"으로 표시한다
  (기본이 꺼짐이라, 안 쓰는 사람에게 빨간불을 띄우면 멀쩡한 앱이 고장 난 것처럼 보인다)
- 상태 점은 색뿐 아니라 글리프(✓ / ! / ·)도 함께 바뀐다

### 브랜드 테마
- **아맞다주차!**: 런처·위젯·퀵설정 타일·알림 채널까지 같은 이름으로 표시
- **야간 아스팔트 + 유도선 노랑**: 어두운 바탕에서 지금 주차한 위치와 선택한 구역이 즉시 눈에 들어오는 테마
- **새 아이콘**: 위치 핀 안의 느낌표로 "아, 맞다!"를 표현

### 첫 실행 온보딩
- 4단계로 **권한 → 차량 블루투스 → 주차장 이름 → 격자 구성**을 정하고 시작
- 격자는 층/구역 개수를 버튼으로만 조절하고 미리보기가 즉시 갱신 (타이핑 0회로 완료 가능)
- 차 블루투스는 **페어링된 기기 목록에서 선택** — 이름을 손으로 적어 오타가 나면 자동 알림이 조용히 안 뜨기 때문
- 업데이트 설치는 온보딩을 건너뛰고 기존 설정을 그대로 유지

### 주차 기록
- **층 × 구역 격자**: 대단지 아파트처럼 층만으론 못 찾는 주차장을 위해 구역까지 한 화면에서 탭 한 번에 저장
  - 층 B1·B2 + 구역 A·B → `B1-A`, `B1-B`, `B2-A`, `B2-B` 버튼이 자동 생성
  - 층을 비우면 구역 이름이 그대로 버튼이 되는 목록형 (작은 주차장용)
  - 층 최대 8개 × 구역 최대 8개
  - 열이 많아도 버튼의 최소 터치 영역을 유지하고 격자 전체를 한 번에 가로 스크롤
- **주차장 프로필**: 우리 집·회사·부모님 댁처럼 주차장을 최대 6개까지 만들고, 상단 이름을 탭해 전환
- **프로필별 격자**: 각 주차장마다 층·구역·기타 구역을 별도로 설정
- **구역 편집 (블록)**: 격자 위 **구역 편집**을 탭하면 층·구역·기타 구역이 실제 버튼과
  같은 모양의 **블록**으로 나온다
  - 블록을 **탭** → 이름 바꾸기 · 삭제
  - **길게 눌러 끌기** 또는 접근성의 **앞/뒤로 이동** 동작 → 순서 바꾸기
  - 끝의 **+** → 추가. 상한에 닿으면 + 가 사라진다
  - 한 줄에 최대 4개. 아래 미리보기가 즉시 갱신되고, 화면을 돌려도 편집 중이던 내용이 남는다
  - 예전에는 여러 줄 EditText 세 개였다. "한 줄에 하나씩"을 사람이 지켜야 했고 순서를
    바꾸려면 텍스트를 잘라 옮겨야 했으며, 편집 중인 글자가 어느 버튼이 되는지 안 보였다
- **기타 구역**: 격자 밖의 자리를 작은 버튼으로 표시 (기본은 비어 있음 — 필요할 때만 채운다)
- **직접 위치 입력**: 타 주차장은 주관식으로 위치 저장
- **지금 주차한 곳 카드**: 마지막 기록 구역 + 정확한 시각 + "n분/시간 전" 상대 시각 (화면 켜둔 동안 매분 자동 갱신)
- **최근에 댄 곳**: 현재 주차장·차량의 최근 5개 목록 표시, 아래 줄에서 전체 기록으로 바로 이동
- **전체 주차 기록**: **기록 탭**에서 삭제한 주차장/차량에 속한 기록까지 확인·수정·삭제
- **기록 수정·메모**: 지금 주차한 곳의 **수정** 또는 최근 항목을 탭해 구역·주차장·차량·시각·메모·사진 수정.
  구역을 비우면 "구역 미입력" 초안으로 돌아간다 — "어디였는지 모르겠다"도 정직한 상태다
- **출차 시간 알림**: 지금 주차한 곳 카드에서 30분/1시간/2시간 뒤 또는 원하는 시각으로 알림 설정
  - Android 절전 모드에서는 알림이 조금 늦을 수 있으며, 별도 정확한 알람 권한은 요청하지 않음
- **삭제**: 카드의 "삭제" 또는 수정 화면의 "삭제"로 기록 취소 (확인 다이얼로그 포함)

### 홈 화면 위젯
- 앱을 열지 않고 현재 주차장·현재 차량의 구역 원탭 저장 (6칸)
- 현재 주차장 + 위치 + 시각을 위젯 상단에 표시, 탭하면 해당 맥락으로 앱 열림

### 차량 블루투스 자동 알림
- 차량을 최대 3대까지 등록하고, 상단 설정에서 현재 차량 전환 (블루투스는 페어링 목록에서 선택)
- 페어링 목록에서 고르면 이름과 함께 **기기 주소**를 저장하고, 끊긴 기기는 주소로 먼저 맞춘다.
  같은 헤드유닛 이름("CAR-AUDIO")을 쓰는 차가 둘이어도 구분되고, 주소는 블루투스 권한 없이도
  읽히므로 권한이 빠져도 감지된다. 이름을 직접 입력한 차량은 예전처럼 이름으로 맞춘다
- 등록된 차량 블루투스 연결이 끊기면(= 차에서 내리면) 초안 기록을 남기고 해당 차량의
  "어디에 대셨어요?" 알림. 알림의 시각은 게시 시각이 아니라 **차에서 내린 시각**이고,
  무시하면 24시간 뒤 스스로 사라진다 (초안은 남는다)
- 알림에 **해당 차량·현재 주차장**에서 자주 댄 구역 버튼 두 개와 **직접 입력** 칸이 붙어 있어
  앱을 열지 않고 바로 기록 가능. 본문을 탭하면 잠금 화면 위 빠른 입력이 뜬다
- 어느 경로로 구역을 적어도 초안의 **끊긴 시각과 좌표**가 그대로 남는다. 당시 위치를 얻지
  못했다면 좌표 없이 남고, 끊긴 직후 8초 단발 측위가 잡히면 그 좌표가 붙는다
- 재연결되면 해당 차량 알림만 자동 취소하고 주차를 닫는다 (차량끼리 독립)
- 연결 상태도 차량별로 따로 보관하며, 재부팅·차량 삭제·블루투스 변경 때 오래된 상태를 지운다
- 블루투스 이름을 비우면 수동 기록 전용 차량으로 사용할 수 있음

### 백업 · 복원
- **설정 → 백업 · 복원**에서 기록 전체와 주차 사진을 zip 파일 하나로 내보내고 되돌린다
  (`data.json` + `photos/`). v3.2 이하의 JSON 파일도 그대로 읽는다
- 문서 선택기(SAF)를 쓰므로 저장소 권한이 필요 없다
- 서명 키가 바뀌거나 폰을 바꿔 앱을 지웠다 깔 때 기록을 지키는 유일한 수단
- 가져오기는 현재 기기의 내용을 통째로 덮어쓰고, 예약된 알림도 다시 맞춘다
- "위치로 알림 조절" 스위치도 백업에 들어간다. 이 키가 없는 예전 백업을 되돌리면 지금 값을 유지한다
- 지금 버전보다 새로운 스키마로 저장된 백업은 거부한다 (구조를 모르는 채 덮어쓰지 않는다)

### 위치로 알림 조절 (기본 꺼짐)
- 주차장마다 좌표를 등록해 두면, 차에서 내린 곳이 **등록한 주차장 근처일 때만** 평소대로 알린다
- 그 밖에서는 무음 채널로 내려 알림함에만 남긴다 (회사·마트에서 매번 뜨던 알림 정리)
- **위치를 알 수 없으면 평소대로 알린다** — 알림을 놓쳐 주차 위치를 통째로 잃는 쪽이 훨씬 손해다
- 블루투스 해제 판정에서는 최근의 유효한 마지막 좌표만 한 번 읽으며 계속 추적하지 않는다
  - 집 주차장이 지하라 그 자리에서는 어차피 위성이 안 잡힌다. 지하 진입 직전 좌표로 판정하고
    반경을 300m로 넉넉히 잡는 이유다. 30분 넘은 좌표는 없는 것으로 친다
- 등록된 주차장이 감지되면 그 주차장의 구역 버튼이 알림에 붙는다 (프로필 자동 전환)
- 블루투스 끊김은 브로드캐스트 수신이라 위치 권한을 **"항상 허용"**으로 둬야 한다
- 근접 판정은 `Nearby.java`에 모여 있고 설정 스위치를 끄면 "어느 주차장인가" 판정은 하지 않는다.
  다만 **기록에 붙는 좌표 스냅샷**은 이 스위치와 무관하다 — 위치 권한이 "항상 허용"이면
  블루투스가 끊긴 순간의 마지막 좌표를 한 번 읽어 그 기록에만 붙인다 (위치 탭의 거리·방향용).
  좌표를 아예 읽지 않게 하려면 Android 설정에서 위치 권한을 회수하면 된다

### 설정
- 하단 **설정** 탭. 항목이 주차장 / 차량 / 기록과 데이터로 묶여 있고, 각 줄에
  현재 상태가 부제로 붙는다 (예: 차량 관리 아래에 "내 차 · CAR-AUDIO")
- **주차장 관리**: 추가·이름 변경·삭제 (최대 6개), 현재 주차장의 구역 관리
- **차량 관리**: 추가·전환·수정·삭제 (최대 3대), 페어링 목록에서 블루투스 선택
- **전체 주차 기록**은 이제 별도 탭이다 (프로필·차량을 삭제한 뒤에도 열어 수정·삭제)
- **구역 커스터마이징**: 층·구역·기타 구역을 한 줄에 하나씩 추가·수정·삭제하고 표시 순서 변경
- 층은 0~8개, 구역은 1~8개, 기타 구역은 0~30개까지 저장 (이름은 24자까지, 중복 방지)
- 기본값으로 되돌려도 기존 주차 기록은 유지

### 체크 (습관)
- 반복할 일 등록 → 탭 한 번으로 오늘 체크/해제
- 연속 일수 + **최근 7일 도트**(●●○●●●●)로 한눈에 흐름 파악
- 길게 누르면 리마인더 설정 · 변경 · 해제, 항목 삭제
- **리마인더**: 설정한 시각까지 체크 안 했으면 알림 (이미 했으면 조용히 넘어감)
  - 알림의 **"지금 체크"** 버튼으로 앱을 열지 않고 바로 완료 처리
  - 재부팅해도 알람 자동 재예약되어 유지

### 퀵설정 타일
- 알림바 타일에 현재 주차 위치 표시, 탭하면 앱 열림

## 빌드

### 개발·직접 설치용 APK

```powershell
powershell -ExecutionPolicy Bypass -File build.ps1
```

결과물: `dist\AMatdaParking.apk`

`build.ps1`은 Android SDK 도구(aapt2 → javac → R8 → zipalign → apksigner)로 직접 설치할 APK를 만든다.
버전·SDK·패키지 이름은 전부 `app/build.gradle`에서 읽으므로 두 빌드 경로가 어긋나지 않는다.

### 테스트와 검사

```powershell
.\gradlew.bat testDebugUnitTest lintDebug assembleDebug
```

유닛 테스트는 에뮬레이터 없이 JVM에서 돈다. 격자 역추론과 기록 정규화(`MigrationTest`),
기록 상한·백업 검증(`StoreDataTest`), 격자·칸 크기(`ZoneGridLogicTest`), 습관 연속 기록(`HabitsTest`),
시각 표시(`FmtTest`), 위치 판정(`LocationRuntimeLogicTest`)을 덮는다 — 깨지면 기존 사용자의
데이터가 날아가는 자리들이다.
CI(`.github/workflows/android.yml`)에서 셋 다 돌린다.

### Google Play용 서명 AAB

출시용 업로드 키를 만든 뒤 `keystore.properties.example`을 `keystore.properties`로 복사해 실제 값만 로컬에 입력한다.
키와 비밀번호는 Git에 올리지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File build-release.ps1
```

결과물: `dist\AMatdaParking-release.aab`

Play 출시 절차, 스토어 문구, 개인정보 처리방침 템플릿, 테스터 안내는 [docs](docs/GOOGLE_PLAY_RELEASE.md)에 있다.
R8 유지 규칙은 `rules.pro`에 있다 (새 Activity/Receiver를 추가하면 여기에 -keep 추가 필요).
필요한 것: `ANDROID_HOME`의 SDK (build-tools 35.0.0, platform android-35), `JAVA_HOME`의 JDK.

## 폰에 설치 후 할 일

1. 앱을 한 번 실행해서 **첫 실행 안내 4단계**를 마친다 (권한 → 차 블루투스 → 주차장 → 격자.
   "근처 기기"는 블루투스 감지용이고 "알림"은 기록·출차 알림 표시용)
2. 홈 화면 빈 곳 길게 누르기 → 위젯 → **아맞다주차!** 위젯 추가 (4x3 크기 추천)
3. 알림바를 두 번 쓸어내려 타일 편집(연필 아이콘) → **아맞다주차!** 타일을 끌어다 추가
4. 앱의 **설정 탭 → 자동 감지 준비 상태**에서 "확인 필요"가 남아 있으면 **설정하기**를 눌러 처리한다
   (배터리 제한 없음, 알림 권한 등 — 예전에는 이 항목을 여기 README로만 안내했다)

## 커스터마이징

| 바꿀 것 | 위치 |
|---|---|
| 주차장 추가·전환 | 홈 상단 주차장 이름 또는 **설정 탭 → 주차장 전환** |
| 층·구역 이름·개수·순서 | 격자 위의 **구역 편집** (설정 탭 → 구역 편집도 동일) |
| 차량·블루투스 | **설정 탭 → 차량 전환 / 현재 차량 관리** |
| 출차 시간 알림 | 홈 히어로 카드 아래의 **출차 시간 알림** 또는 기록 수정 화면 |
| 주차 사진 · 음성 메모 | 히어로 카드 아래의 **사진**, 기록 수정 화면, 알림의 빠른 입력 |
| 삭제한 주차장·차량의 기록 | **기록 탭** |
| 자동 알림이 안 뜰 때 | **설정 탭 → 자동 감지 준비 상태** (홈의 감지 상태 카드를 눌러도 이동) |
| 차를 어디 뒀는지 찾기 | **위치 탭** (보고 있는 동안 내 위치가 따라온다) |

앱 화면에는 격자 전체가 표시된다. 홈 위젯은 크기가 고정되어 6칸까지만 표시하는데, 격자가 그보다
크면 **최근에 쓴 구역**을 골라 담는다. 다만 배치는 격자 순서를 그대로 지키므로 주차할 때마다
버튼이 자리를 바꾸지 않는다. 직접 입력한 일회성 위치는 위젯에 올라오지 않는다.
6칸보다 적게 설정하면 남는 버튼과 행을 자동으로 숨긴다.

## 주의

- **`debug.keystore`는 개인 서명 키다.** Git에 올리거나 공유하지 말고 안전한 곳에 백업한다. 키가 바뀌면 앱을 지웠다 다시 깔아야 하는데, 그 전에 **설정 → 백업 · 복원 → 파일로 내보내기**를 해 두면 기록을 그대로 되살릴 수 있다.
- 업데이트 버전을 만들 때는 `app/build.gradle`의 `versionCode`를 1 올리고 다시 빌드 → 폰에서 덮어서 설치 (기록 유지됨).
  `build.ps1`도 이 값을 읽으므로 두 곳을 맞출 필요가 없다.

## 구조

```
app/
  AndroidManifest.xml
  src/com/ohdduck/parknote/  # 기존 설치 업데이트 호환성을 위한 내부 패키지
    MainActivity.java        # 탭 네 개의 껍데기 + 홈 탭 그리기 (히어로·감지 상태·격자·체크·최근)
    QuickRecordActivity.java # 잠금 화면 위 빠른 입력 (알림 탭 → 구역 탭)
    Photos.java              # 주차 사진 저장·축소·정리·백업 스테이징
    PhotoProvider.java       # 카메라 앱이 사진을 쓸 content URI (FileProvider 대신)
    Voice.java               # 음성 인식 → 메모
    Tabs.java                # 하단 탭 선택 상태와 화면 전환 (Fragment 없이 visibility)
    HistoryTab.java          # 기록 탭 — 저장된 주차 기록 전체
    LocationTab.java         # 위치 탭 — 거리·방향·좌표·나침반 (지도 타일 없음)
    SettingsTab.java         # 설정 탭 — 준비 상태 카드 + 설정 항목
    ReadyCheck.java          # 자동 기록이 동작할 조건 판정 (권한·배터리·블루투스)
    OnboardingActivity.java  # 첫 실행 4단계 안내 (권한·차량·주차장·격자)
    ZoneSettingsActivity.java# 층·구역·기타 구역 블록 편집 + 미리보기
    ZoneBlocks.java          # 블록 격자 렌더링 (탭 수정 · 끌어 이동 · + 추가)
    ScreenHost.java          # 다이얼로그 → 화면 갱신 통로
    Ui.java                  # 폼·다이얼로그 공용 조각 (입력 필드 스타일 한 곳)
    RecordEditor.java        # 주차 기록 수정 + 주차장/차량/시각/출차 알림 선택
    ProfileDialogs.java      # 주차장 전환·추가·이름 변경·삭제
    VehicleDialogs.java      # 차량 전환·추가·정보 수정·삭제
    HabitDialogs.java        # 체크 항목 추가·리마인더·삭제
    BackupFlow.java          # 백업 내보내기·가져오기 흐름 (파일 I/O는 워커 스레드)
    LocationFilterDialogs.java # 위치로 알림 조절 설정·권한 요청
    ZoneGrid.java            # 층 × 구역 격자 렌더링 (메인·온보딩·구역 편집 공용)
    BtPicker.java            # 페어링된 기기 목록에서 차 블루투스 선택 (이름 + 주소)
    Backup.java              # 기록 전체를 JSON 파일로 읽고 쓰기 (문구 없이 결과만)
    Nearby.java              # 위치 권한·마지막 좌표·단발/연속 측위·거리·주차장 판정 (위치 코드는 전부 여기)
    Store.java               # 주차장 프로필·차량·주차 기록 (저장값 파싱 캐시 포함)
    Migration.java           # 이전 버전 데이터 이관 (v2.2 키, 평면 구역 목록 → 격자)
    Habits.java              # 체크 항목 (Store와 저장소만 공유)
    Fmt.java                 # 시각 표시 (해가 다르면 연도 표시)
    Notify.java              # 알림 채널·ID·취소 (채널은 앱 시작 때 미리 만든다)
    Json.java                # 저장 JSON 도우미
    ParkWidgetProvider.java  # 홈 화면 위젯
    BtReceiver.java          # 차량 블루투스 끊김 → 초안 기록 + 알림 (+8초 단발 측위), 재연결 → 출차
    ParkingTimers.java       # 기록별 출차 알림 예약/해제
    ParkingTimerReceiver.java# 출차 알림 수신·표시
    Reminders.java           # 습관 리마인더 알람 예약
    ReminderReceiver.java    # 알람 수신 → 미체크 시 알림, 부팅·앱 업데이트 후 재예약
    ParkTileService.java     # 퀵설정 타일
  res/values/strings.xml     # 사용자에게 보이는 문구 전부
  res/                       # 레이아웃, 위젯, 색, 아이콘
  test/com/ohdduck/parknote/ # JVM 유닛 테스트 (격자 추론·마이그레이션·연속 기록·백업 검증)
build.ps1                    # 원커맨드 빌드 스크립트 (버전은 app/build.gradle에서 읽음)
build-release.ps1            # 업로드 키로 Google Play용 AAB 생성
app/build.gradle             # 출시 서명·AAB를 위한 Gradle 설정
docs/                        # Play Console·스토어·개인정보·테스트 자료
rules.pro                    # R8 유지 규칙 (새 컴포넌트 추가 시 -keep 필요)
```

데이터는 SharedPreferences에 저장:

- 새 주차 기록: `history` = `[{id, c: 차량ID, cn: 차량명, p: 주차장ID, pn: 주차장명, z?: 구역(없으면 초안), t: 내린 시각, e?: 출차 시각, m?: 메모, ph?: 사진 파일, due?: 출차알림시각, lat?: 위도, lon?: 경도, loc_t?: 좌표측정시각, loc_acc?: 정확도(m)}]`
  (주차장×차량 맥락마다 20개, 전체 240개까지. 전역 40개였을 때는 차를 두 대 쓰면 한쪽 기록이 다른 쪽을 밀어냈다)
- 사진: `files/photos/<기록 id>.jpg`. 기록과 함께 지워진다
- 습관: `habits` = `[{n: 이름, r: 리마인더(0시 기준 분, -1=없음), days: ["yyyy-MM-dd" 최신순], lt: 오늘 체크 시각}]`
- 설정: `parking_profiles_v1` = `[{id, n, rows: 층[], cols: 구역[], sep, etc: 기타구역[], lat?, lon?, rad?}]`,
  `parking_vehicles_v1` = `[{id, n, b: 블루투스 이름, a?: 블루투스 주소}]`, `bt_state` = 차량별 최근 연결 상태,
  `onboarded` = 첫 실행 안내 완료 여부, `location_filter` = 위치로 알림 조절 사용 여부

기록에 저장되는 `z`는 격자를 편 문자열(`"B2-C"`)이라 이전 버전의 기록과 그대로 호환된다.

v2.2 이하의 단일 구역 목록·차량 이름·기존 주차 기록은 처음 실행할 때 기본 주차장/기본 차량으로 자동 이관된다.
v2.5 이하의 평면 구역 목록(`main`)은 스키마 v5에서 층×구역 격자로 역추론된다. 완전한 곱집합이
아니면(예: `B1-A, B1-B, 지상`) 사용자가 쓰던 목록을 그대로 보존하는 1차원 구성으로 남는다.
스키마 v6(v3.3)은 구조 변환 없이 초안·출차 시각·사진 필드를 더한 것이라, v5 데이터는 그대로 읽힌다.

표시 이름은 **아맞다주차!**로 바뀌었지만, 기존 설치에서 기록·위젯·예약 알림을 그대로 이어가기 위해
내부 패키지명과 저장 키는 유지한다.
